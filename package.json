{
  "name": "to-excel",
  "version": "2.1.0",
  "description": "Util to generate compatible Excel xls file (xml in fact). Works with node and browser. CommonJS and ES6 module compatible.",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha",
    "coverage": "nyc --reporter=lcov --reporter=text-lcov npm test",
    "coveralls": "cd travis && node ./build.js && nyc npm test && nyc report --reporter=text-lcov | coveralls",
    "build": "cd travis && node ./build.js"
  },
  "keywords": [
    "export",
    "excel",
    "ES6",
    "CommonJS",
    "xls",
    "xml"
  ],
  "author": "m0rtadelo",
  "bugs": {
    "url": "https://github.com/m0rtadelo/to-excel/issues"
  },
  "homepage": "https://github.com/m0rtadelo/to-excel",
  "repository": {
    "type": "git",
    "url": "https://github.com/m0rtadelo/to-excel.git"
  },
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.6",
    "mocha": "^5.2.0",
    "nyc": "^14.1.1"
  }
}
